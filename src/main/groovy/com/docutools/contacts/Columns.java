package com.docutools.contacts;

public enum Columns {
    Email,
    Username,
    CompanyName,
    FirstName,
    Name,
    LastName,
    Phone,
    Fax,
    JobTitle,
    Department,
    InternalId,
    Street,
    Zip,
    City,
    CountryCode,
    /**
     * Indicates that this column shall be ignored by the import.
     */
    Ignore
}
